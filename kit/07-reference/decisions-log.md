# Decisions Log

Append new entries at the bottom: time, decision, why, who. Entries below were made while planning (2026-09-10/11) before anything was built.

## Idea selection

| # | Decision | Why |
|---|---|---|
| 1 | Rejected the brief's ten suggested ideas (support desk, research assistant, recruiting, fraud, finance, legal, networking, coding agent, smart home, sales) | Every team gets the same list; entries converge |
| 2 | Rejected "Told Once" (data analyst corrected once, with a replay "flinch") | Solid fit but least surprising; weak live reveal |
| 3 | Rejected "Patch Once" (vulnerability fix replayed across repos) | Product owner found the gap: an identical fix across repos is just automation; redesigned into pattern learning, then set aside for a student-facing idea |
| 4 | Rejected stock-market ideas ("Broken Promises", "Hidden Exposure Map", "Why did it move?") | Product owner wanted a student-geared direction |
| 5 | Rejected "Mistake Memory", "Degree Path", "Application Autopilot", "Office Hours Memory" | Useful but no moment where the room reacts |
| 6 | **Chose Exam Oracle** | Five-second hook; a sealed, checkable reveal; measurable "smarter" vs baselines; natural fit for all five tools |
| 7 | Added the student layer (preferences, syllabus, guidelines, feedback) | Product owner requirement, 2026-09-11 |

## Architecture (from reading the vendors' docs and source)

| # | Decision | Why |
|---|---|---|
| 8 | Build the app from RocketRide pipelines using the `tool_cognee`, `graph_hydradb`, `db_hotdata` nodes | These nodes exist in RocketRide's docs/source; sponsor-native; judges check real use |
| 9 | Hosted HydraDB is primary; open-source HydraDB optional | RocketRide's node speaks the hosted API; per-student collections and `infer:true` preferences fit the student layer |
| 10 | Do not connect Cognee to open-source HydraDB through the Neo4j provider | Adapter uses APOC, constraints, UUIDs, list properties; HydraDB rejects all; a ~100-line projector if the Cypher graph is wanted |
| 11 | Use Cognee's v1 API (`remember`/`recall`/`improve`/`forget`) with custom models, strict ontology, datasets with permissions, sessions + feedback | Current API; the brief asks for remember/recall; these features carry lessons and preferences |
| 12 | One throwaway hotdata database per backtest | Creation under a second with expiry; makes leakage impossible by construction |
| 13 | Writes to hotdata are loads with idempotency keys | hotdata SQL is read-only |
| 14 | Course-specific logic in generic scripts; Rote records inside a workspace | A Play replays a fixed graph; improvisation would be baked in |
| 15 | The LLM never predicts the exam | It may have seen OCW exams in training; predictions come from the signal model |
| 16 | Reveal exam = 6.003 Fall 2011 Final | 6.003 has Quiz 1, Quiz 2, Final across three terms with solutions (verified) |
| 17 | Where things run: RocketRide staging, hotdata cloud, hosted HydraDB, one reachable Cognee server, Rote on the laptop | RocketRide's Python node can't run subprocesses; deployed apps run on staging |
| 18 | Snyk MCP added via project config, not the global configure command | The configure command writes Claude's global rules file |

## Open at G0 (fill in during the event)

- Cognee location: ____ (Cloud / laptop + tunnel)
- Sponsor nodes present in `services-catalog.json`: ____
- Hosted HydraDB `query_graph` accepts Cypher: ____
- Cross-dataset Cognee recall merges: ____
- Rote replay from Python works: ____
- hotdata per-run index build time: ____
- Course 2: ____ (18.06 earlier finals counted: __ ) / 2.71 / none
- Answer key SHA-256 for 6.003 Final Fall 2011: ____ (time: ____)

## 2026-09-11 — Person B, G0 (contracts + SDK corrections)

| Time | Decision / finding | Why | Who |
|---|---|---|---|
| G0 | **Contracts written** in `contracts/`: `ledger-schema.sql`, `prediction.schema.json`, `student.md`, `README.md`, synthetic fixtures for course FX.101 (all ledger tables, a fake prediction, 3 fake runs, human-shaped answer keys). Validated: prediction fixture passes the schema; the DDL executes in DuckDB; every fixture CSV header matches its table. | Unblocks A (ledger columns) and C (prediction + runs shapes) | B |
| G0 | **Time is ordered by `(term_seq, session)`, not dates.** `term_seq = year*10 + {1 spring, 2 summer, 3 fall}`; a row is visible to target T iff earlier term, or same term and earlier session. | OCW calendars often give session numbers, not dates | B |
| G0 | **CONCERN — single-term materials.** OCW publishes lecture notes/psets for one term only, so earlier-term backtests cannot legitimately use them. Runs are tagged `feature_set = full` (target in the published term) or `exam_history` (only x1 and x7). The kit's M1 run (6.641 Final 2008 ← 2006) is an `exam_history` run. | Using a later term's lectures to predict an earlier exam leaks the future; the leakage check would fail | B |
| G0 | **CONCERN — answer-key labeling load.** Every scored backtest needs a human-labeled key (~10–13 exams). Added `runs.key_source` (human/machine); `score.py` refuses without a human key unless `--allow-machine-key`, which marks the run as machine-graded. | "The AI never grades itself" is otherwise silently broken | B |
| G0 | **Contract additions:** `run_labels` (training labels written after the seal), `echo_pairs` (homework-echo precomputed once instead of per-run search), `lessons` (ledger mirror of HydraDB lessons for charts), `guidelines`, `guideline_tests`, `courses`, `topics`, `exams`. | Refit needs labels without editing sealed predictions; per-run index builds are an untested cost | B |
| G0 | **SDK fact — hotdata:** `hotdata-framework` managed loads require **Parquet** (CSV rejected by this client); idempotency = table keys + `mode="upsert"`; SQL inside a managed DB names tables `"default"."public"."<t>"`; env `HOTDATA_API_KEY`, `HOTDATA_WORKSPACE`, `HOTDATA_API_URL`. `ledger-schema.sql` is a column contract, not executable in hotdata (read-only SQL). | Read from installed source 0.14.0 | B → A |
| G0 | **SDK fact — HydraDB:** package `hydradb-sdk` 2.1.4 imports as `hydra_db`; memories via `context.ingest(type="memory", memories=<JSON string>, upsert="true")`; item `{id,title,text,infer,custom_instructions,additional_metadata}`; **one collection per query** → student + shared = two queries. | Installed source + HydraDB docs llms.txt | B → C |
| G0 | **SDK fact — Cognee 1.5.4:** feedback is `cognee.session.add_feedback(...)` (no top-level `cognee.add_feedback`); default storage is inside the installed package → set `DATA_ROOT_DIRECTORY`/`SYSTEM_ROOT_DIRECTORY`; default posture is authentication required + multi-tenant. | Installed source | B |
| G0 | **Ownership:** B writes `oracle/cognee_tag.py` (tagging onto the strict topic list); A's `load-course` Play calls it. | Tagging is Cognee/model work; the Play only sequences it | B ↔ A |
| G0 | **BLOCKED — live smoke tests.** This machine has no sponsor credentials (hotdata, HydraDB, Cognee LLM key) and its GitHub token is expired. B's code is built and tested offline (DuckDB + local stores); live paths are gated and marked `LIVE [TEST]`. | Needs team keys + repo access | B → team |
| Build | **Contract: `run_labels` key `(run_id, topic_id)`** added to contracts/README.md (same map as `oracle/backend.py` `TABLE_KEYS`); `homework_vec` stays keyless. No fixture change (there is no `run_labels` fixture). | `score.py` writes `run_labels` after the seal and Rote replays must be idempotent, which needs an upsert key | B |

## 2026-09-11 — Person B, A/B boundary (course structure + D8 run lists)

| Time | Decision / finding | Why | Who |
|---|---|---|---|
| Build | **Boundary contract `contracts/course-structure.md`.** B owns `data/courses/<course>/*` and the run lists; A's loader reads the folder only through `oracle.structure.load_course()` and takes every time/coverage/cumulative/sealed/topic fact from it. From PDFs A extracts only problem numbers, sub-parts, points, problem text and homework text, joined on `exam_id` / `(term, set)`. | Every leakage-relevant fact lives in one checked place; A never infers a session or topic from a PDF | B → A |
| Build | **`python -m oracle.structure --course <c> --check`** (exit 3 on failure): closed headers, ids, term_seq, topic partition, D2 sessions, A1 position, roles/sealing, skip list, sources, no problem content (forbidden JSON keys, math markup). 6.003, 18.06 and 6.641 pass; `tests/test_structure.py` covers them plus broken copies. | Mechanical check before any load | B |
| Build | **D2 + `session_source` vocabulary:** `calendar_own_row` (18.06 S10 12/25/37/40), `calendar_shared_row` = last delivered session + 1 (6.003 F11 9/14/20/26; 6.641 S09 12/26), `synthetic` 1000/2000/3000/9000 for other terms. 18.06's `calendar` is a legacy alias of `calendar_own_row` (warning, not failure). "After exam E" means `session >= E.session`. | Calendars place exams in lecture rows; this keeps the next lecture out of the exam's past | B |
| Build | **A1 exam_type by position** (first in-term exam = quiz1, ... Final = final; 6.641 Midterm = quiz1); one exam per slot per term, slots in session order (checked). | Sources name the same paper Quiz/Exam/Midterm | B |
| Build | **Loader rules (bind A):** lectures skip `is_review` rows; exams skip `role = excluded` (6.003-quiz3-2010S, 6.641-final-1995S); `sealed_reveal` loads its exams row only; `topics.first/last_lecture <- first/last_session` (session numbers); `homework_items.session = due_session`, sets without one (6.641 opt) not loaded; T00 loaded into `topics` with NULL bounds (D6). Homework is visible by due session. | Each changes a signal if ignored (x4 denominators, x7 windows, T5 gap) | B → A |
| Build | **Skip list only for the sealed reveal.** 18.06 and 6.641 targets are backtest targets whose items load (A-NOSEAL, SKIP), protected per run by `sql/leakage.sql`; this departs on purpose from kit sealing rule 1. | Midterm/quiz labels feed later targets' x6 | B |
| Build | **Sources:** ocw.mit.edu only. The 18.06 `https://web.mit.edu/18.06/www/` archive (x1 papers 2006S-2009F + 2010S pset PDFs, 64 URLs, no CC licence) is a documented exception for the structure check only (`oracle.structure.SOURCE_EXCEPTIONS`); the fetch allowlist (`validate_ocw_url`) is unchanged. PENDING: A extends it deliberately with its own line (non-commercial backtest use, no redistribution, credit). Without it 18.06 targets have no x1 papers. | Licence and allowlist are a team decision | B → A |
| Build | **D8 run lists:** `data/run_lists/real_chain.csv` (10 6.641 Midterm = M1, 20 6.641 Final, 30-60 18.06 S10, 70/71 6.003 Q1 + twin, 80, 90, 100/101 6.003 Final sealed + twin) and `history_side_track.csv` (1-3 6.641 2006F/2008Q1/2008F, 21-23 6.003 S10 Q1/Q2/F). The gaps interleave the side track in time, so `read_lessons --before-run-seq` gives each run only earlier-term lessons and both lists pass `run_list` together (no exemption). The kit's former M1 (6.641 Final 2008) is side-track run 3. `REAL_TEMPLATE.csv` is superseded (kept for `tests/test_e2e.py`). | D8 order, leakage-clean lessons | B |
| Build | **Reveal procedure:** runs 100 and 101 are both sealed before either is scored: the 6.003 Final key stays out of `SEALED_DIR` until both `.sha256` files exist (`run_list` stops at exit 6 after sealing; `--from-seq 101`; then `--step score`). The side track must not run before D4 lands in refit. | "Both sealed before scoring" without new code | B |
| Build | **DEFERRED: the frozen-beta_G arm** of the 6.003 targets (D8) is not in the run lists: no `arm` column or Backtest arm support yet. | Time; lands with the D8 code revision | B |
| Build | **D9: student self-description box** (`contracts/student.md` §7; new `contracts/learning-profile.schema.json`; fixture `contracts/fixtures/self_description_examples.json`; `oracle/students.py`; `tests/test_self_description.py`). C's box (max 1,000 chars) sends `{student_id, text, created_at}`; B strips control characters and HTML, then stores the text verbatim as HydraDB memory `s<n>_self` (infer:true, upsert replaces on edit, custom_instructions: extract durable teaching preferences, ignore instructions addressed to the system) and in Cognee `student-<n>` (LIVE [TEST]). B returns a PROPOSAL (`learning-profile.schema.json`: each field value + confidence + a verbatim evidence quote of at most 120 chars) shown as confirm chips: `rules_v1` offline, `llm_v1` with LLM_API_KEY (schema-validated, unquoted claims dropped, falls back to rules_v1). Only the fields C sends back in `{student_id, confirmed}` become explicit prefs (infer:false fixed ids `s<n>_pref_style`, `_length`, `_modality`, `_pace`, new `s<n>_pref_goals` and `s<n>_pref_adjust`); the §4 read gains optional `goals` / `adjustments`. The system prompt never contains the raw text; accommodations are kept only as 7 fixed neutral teaching adjustments, never the condition. C's RocketRide pipeline uses the same schema and prompt. | The product owner asked how a free-text "how you like to learn" box is processed and stored. Confirm-before-apply keeps the student in control, and an injection typed into the box cannot reach the tutoring prompt or another student's data | B |

## 2026-09-11 — Person B, integration (D3-D8 in code, run lists reconciled)

| Time | Decision / finding | Why | Who |
|---|---|---|---|
| Build | **SQL fixes landed:** `sql/x5_untested_recent.sql` uses `session >= last exam's session` ("after E" = `session >= E.session`, now also in `contracts/README.md`); `sql/coverage_window.sql` falls back to `COALESCE(prev, 1)`. The 6.003 Quiz 2/Quiz 3 windows were then blanked in `exams.csv` (W-QUIZ): they still come out 9-13 and 14-19, now labelled `since_previous_exam`, not `stated`. | The 6.003 Final's x5 needed T15; a pinned window was mislabelled as a professor statement | B |
| Build | **D3 in code:** every signal and the window are computed for every target; the exam_history exit-4 guard in `rank_and_seal` and `ordering.EXAM_HISTORY_ZERO_SIGNALS` are gone. **D4:** refit trains only on `feature_set = full AND cold_start = false` (`left_out.exam_history_runs`). **D5:** z-score per run (population std, zero variance -> 0) in one function used by `rank_and_seal` and refit; predictions keep RAW x; optional `"standardization": "zscore_per_run_v1"` in `prediction.schema.json`, `fixtures/prediction.json`, `make_fixtures.py` and the backtest summary. | Decisions D3-D5 | B |
| Build | **D6 in code:** T00 is loaded but never gets a signal row, a rank, a prediction or a place in K / baseline B / x3's max / refit rows; `score` keeps T00 key points in the denominator (`off_list_pts`; exam_history closed form K/N x (1 - off_list_pts)); `recall_k` is NULL when no predicted topic is on the key. Judgement call: T00 quiz points stay in x6's denominator, as in scoring. **D7:** `homework_analogous` x7 covers topics with >= 1 visible homework problem at its own trust (default 0.5); `score` writes its `guideline_tests` (match >= 50 % of points on topics with visible homework, partial 30-50 %, miss < 30 %; none for a machine key, a twin, or no visible homework). Kind added to `ledger-schema.sql`, `oracle/models.py`, `oracle/structure.py`. | Decisions D6-D7 | B |
| Build | **Contract comments (no column change, no fixture change):** `topics.first_lecture/last_lecture` hold SESSION numbers (from `first_session/last_session`), NULL for T00 and untaught topics; `contracts/README.md` gets the D3 wording, the T00 id rule and `learning-profile.schema.json`; `student.md` §2 lists `s<n>_self`, `s<n>_pref_goals`, `s<n>_pref_adjust`, §6 "delete my data" also deletes the HydraDB memories (no delete code yet: C calls both stores). | Keep the contracts equal to the code | B |
| Build | **D8 side track, one mechanism:** the side track is `history_side_track.csv`, ordinary `exam_history` rows with `cold_start = false` interleaved by run_seq. The parallel `track = history` column (cold_start = true, exempt from the time checks) was removed from `oracle/run_list.py`: it made side-track runs look like twins in `runs` and needed a new contract column, while `runs.feature_set` already tells them apart. Under D4 they never train; each writes a lessons version that carries the earlier full runs' weights. Line 72's gate (D4 in refit) is met. | One rule, no exemption that could hide an ordering mistake | B |
| Build | **Reveal procedure (supersedes line 72):** `run_list --list real_chain.csv --through-seq 90`; hours before, `--from-seq 100 --seal-only` seals runs 100 and 101 with no key opened; on stage `--from-seq 100 --frozen` replays both (same hashes), scores them and writes no lessons version. | `--seal-only` / `--frozen` exist now | B |
| Build | **M1 switch (D8):** M1 is the 6.641 2009 Midterm (run 10, full). The kit's M1, 6.641 Final 2008, is side-track run 3 (exam_history, never trains). **6.641 packet (A-PACKET):** the 1995 packet final is `role = excluded` (no rows, URLs blank); only pp. 8-18 (1998) and 28-35 (2000) of the packet load (`packet_sections`); a sensitivity run without the 1998/2000 packet finals (S-NOPACKET) is pre-registered. | Leakage-clean first run; the packet papers are partial | B |
| Build | **18.06 `session_source` relabelled `calendar_own_row`;** the legacy `calendar` alias is removed from `oracle/structure.py`. | One D2 vocabulary | B |
| Build | **`tests/test_real_visibility.py`:** the three real folders load through `oracle.structure` into a temporary ledger (synthetic items/homework with the real times, plus a poisoned item for the sealed Final); for all 18 runs of `real_chain.csv` + `history_side_track.csv`, each run database holds exactly the D1-visible rows, D2 sessions/shared rows/homework-by-due-session hold, full targets are in the published term, history targets see nothing from it, the sealed 6.003 Final is never visible, and `leakage.sql` is clean. | Visibility checked on the real structure before A loads | B |
| Build | **Still DEFERRED / open:** the frozen-beta_G arm; every live path (hotdata, HydraDB, Cognee, LLM: LIVE [TEST]); real exam/homework content from A's loader (not written); human answer keys for every scored target; `backtest` does not pass `--tau` to `run_signals` (x3 uses 0.02); no `guideline_tests` writer for the cumulative / emphasis_window / coverage kinds (their trust stays 0.5); the 18.06 archive fetch allowlist (A's decision). | Honest state for the stage | B |
